{
  "private": true,
  "scripts": {
    "build": "parcel build src/index.html",
    "start": "parcel serve src/index.html --https",
    "start:bootstrapper": "npm run clean && npm run build && cd dist && zip -r dist.zip . && serve -p 1234",
    "clean": "rimraf dist .parcel-cache",
    "test": "exit 0"
  },
  "devDependencies": {
    "@types/react": "^18.0.27",
    "@types/react-dom": "^18.0.10",
    "parcel": "^2.8.3",
    "process": "^0.11.10",
    "serve": "^14.2.0",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
